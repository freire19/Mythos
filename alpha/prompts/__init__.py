"""Bundled system + sub-agent prompts.

Accessed via `alpha._resources.package_data("prompts", "system.md")` etc.
This file exists so the `alpha.prompts` directory is recognized as a
Python package by setuptools, which is the wheel-friendly way to ship
the `.md` files for `importlib.resources`.
"""

from __future__ import annotations

